# Claude Agent Team

Agent Team 활성화 후 아래 역할 파일을 기준으로 작업을 나눕니다.

- `agents/backend.md`: Java/Spring MVC/MyBatis 연동
- `agents/frontend-jsp.md`: JSP/CSS/JavaScript
- `agents/db-designer.md`: DB 설계, DB별 MyBatis XML, DB 설정
- `agents/qa.md`: 최종 점검 전용

작업 시작 전 루트의 `CLAUDE.md`와 `BluePrint.md`를 함께 확인합니다.